# 개발 계획서: AI 기반 소셜 미디어 콘텐츠 자동화 시스템 - 프로세스 흐름

## 1. 프로젝트 목표
구글 드라이브(로컬 동기화 폴더)에 업로드된 이미지를 AI가 분석하여 고품질 스토리텔링 문구를 생성하고, 텔레그램 승인 과정을 거쳐 페이스북/인스타그램에 자동 게시하는 시스템 구축

## 2. 전체 프로세스 흐름 (Work-Flow)

1.  **애플리케이션 실행:**
    *   `main.py` 파일 실행 (하나의 명령으로 모든 기능 시작).

2.  **이미지 감지 및 배치 처리:**
    *   **초기 스캔:** 실행 즉시 감시 폴더(`.env`에서 읽음)를 스캔하여 기존 이미지 파일을 감지합니다.
    *   **실시간 감시:** `watchdog`가 새로운 이미지 파일 생성을 실시간으로 감지합니다.
    *   **파일 안정성 대기:** 감지된 파일이 완전히 복사될 때까지 대기합니다.
    *   **배치 수집:** 일정 시간(`10초`) 동안 감지된 파일들을 '장바구니'에 수집하여 하나의 배치(Batch)로 묶습니다.
    *   **파일명 일괄 변경:** 배치로 수집된 이미지 파일들의 이름을 순차적으로 `no1.jpg`, `no2.png`, `no3.jpg` 등으로 변경합니다 (확장자는 원본 유지).

3.  **텔레그램으로 컨텍스트 요청:**
    *   배치 처리가 완료되면, 텔레그램 봇이 사용자에게 감지된 이미지(첫 번째 이미지를 대표로)를 보내고 메시지를 전달합니다.
    *   **메시지:** "새 이미지 [N]개가 감지되었습니다. 게시글을 생성할까요?"
    *   **버튼 옵션:**
        *   **[✍️ 정보 추가하기]:** 사용자로부터 텍스트 컨텍스트(장소, 경험 등)를 입력받습니다.
        *   **[🤖 이미지로만 생성]:** 추가 정보 없이 이미지 분석으로 바로 진행합니다.

4.  **사용자 응답 처리:**
    *   **[정보 추가하기] 선택 시:** 봇이 "사진에 대한 설명을 자유롭게 입력해주세요."라고 안내하며, 사용자님의 다음 텍스트 메시지를 기다립니다.
    *   **[이미지로만 생성] 선택 시:** "AI에게 전달할 준비를 마쳤습니다."라고 안내하고 다음 단계로 진행합니다.
    *   **텍스트 컨텍스트 입력 시:** 봇이 사용자님의 설명을 컨텍스트로 저장하고 "AI에게 전달할 준비를 마쳤습니다."라고 안내하며 다음 단계로 진행합니다.

5.  **AI 초안 생성 및 캐싱:**
    *   **캐시 확인:** 이미지 배치에 해당하는 `.json` 파일이 이미 있는지 확인합니다.
    *   **캐시 히트 시:** 기존 `.json` 파일에서 초안 내용을 로드합니다 (AI 호출 생략).
    *   **캐시 미스 시:**
        *   이미지(들)와 수집된 컨텍스트(또는 컨텍스트 없음)를 멀티모달 AI(Gemini)에 전송합니다.
        *   AI는 지정된 JSON 형식(`facebook_text`, `instagram_text`, 해시태그 등)으로 게시글 초안을 생성합니다.
        *   생성된 JSON 초안을 이미지와 같은 폴더에 `.json` 파일로 저장합니다.

6.  **텔레그램으로 AI 초안 전달 (TODO):**
    *   생성되거나 캐시에서 로드된 AI 초안(JSON)의 내용을 사용자에게 텔레그램으로 전송합니다.

7.  **텔레그램 최종 승인 및 피드백 (TODO):**
    *   전달된 초안에 대해 사용자로부터 [승인]/[수정]/[거절] 등의 피드백을 받습니다.

8.  **RPA를 통한 소셜 미디어 게시 (TODO):**
    *   최종 승인된 초안을 Facebook/Instagram에 자동 게시합니다.

---